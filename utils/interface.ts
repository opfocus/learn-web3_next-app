export interface IsDepositModal {
  depositModal: boolean;
  setDepositModal: (value: boolean) => void;
}

export interface IsWithdrawModal {
  withdrawModal: boolean;
  setWithdrawModal: (value: boolean) => void;
}
